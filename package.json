{
  "name": "sync-evolve",
  "version": "1.0.0",
  "description": "Synchronicity Engine - Sacred Technology for Regeneration",
  "type": "module",
  "scripts": {
    "dev": "echo '🚀 Start local server: python3 -m http.server 8000' && python3 -m http.server 8000",
    "build": "node scripts/build-simple.js",
    "deploy": "npm run build && git add . && git commit -m 'deploy: Update GitHub Pages' && git push",
    "verify-deployment": "node scripts/verify-deployment.js",
    "health-check": "echo '🏥 Health check available at: https://syncengine.earth/health.html'",
    "edit-info": "echo '📝 Edit files in src/ directory (single source of truth)'"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vite-plugin-html": "^3.2.2"
  }
}
